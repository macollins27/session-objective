---
description: Print this session's objective file
allowed-tools: Read
---

Show the operator this session's objective file, verbatim. Do not summarise it, do not
comment on it, and do not change it.

The path is named in the `SESSION OBJECTIVE` block in your context, on the line
beginning `file:`. Use the **Read** tool on exactly that path — a Read of this session's
own objective file is always permitted.

Do not reach for the shell: every Bash command that names the objective home is denied,
and there is no script to run.

There is no set, revise, decide or complete command. The OPERATOR LEDGER holds what he
typed and nothing else; the OBJECTIVE is written from it by a call that has never seen
this session; PROGRESS is the only layer you write.
