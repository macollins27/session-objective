# A1 — replay: every ACTIVE-ended ledger through the real 3.0 interpreter

Run 2026-09-13. Every objective file under `~/.session-objective/sessions` whose STATUS ended
`ACTIVE` had its OPERATOR LEDGER copied out, rebuilt as a fresh 3.0 file with an empty OBJECTIVE,
and passed to `scripts/objective-interpret.sh` — the real interpreter, the real `claude -p` call,
`SESSION_OBJECTIVE=off` in the child. Every produced document was then re-validated independently
with `scripts/objective-validate.sh`. **Nothing under `~/.session-objective/sessions` was modified:**
the replay reads each file and writes only into the scratchpad.

Runner: `scratchpad/vt/replay.sh`; raw results `scratchpad/vt/replay/results.tsv`; each produced
file kept beside it as `<id>.md`.

## Counts

| | count |
|---|---|
| ledgers replayed (STATUS ended ACTIVE) | 23 |
| interpreted, and **validator PASS** on the produced OBJECTIVE + WORKFLOW | 22 |
| interpreter produced no usable answer (file left unchanged) | 1 |
| of those replayed, files carrying at least one operator message | 8 |
| of those replayed, subagent files with an empty ledger | 15 |

Median wall time per interpretation: 5 s.

### The one failure, stated rather than smoothed over

```
8ace0f5b-9bc8-44d8-9d6c-b2c8c295b3eb  (10 ledger entries)
  the answer did not pass validation: the objective and workflow are 314 words together; the cap is 280
```

A ten-message ledger produced a 314-word OBJECTIVE + WORKFLOW against the 280-word cap, twice
running. The hook then leaves the previous OBJECTIVE exactly as it was, says so in one visible
line, and the next operator message retries; the Stop gate refuses COMPLETE while the binding lags
the ledger. No partial objective is ever written. On long ledgers the 280-word cap is reachable,
and when it is reached the session keeps the objective it already had.

### What the 23 actually are

15 of them are subagent copies (`<session_id>/<agent_id>`), which take no operator messages by
design, so their ledgers are empty. The interpreter answers them the only honest way available to
it — a conversation objective whose OPEN QUESTION asks for a first message — and the WORKFLOW is
the single line `none (conversation)`, so no edit lock applies to them. Of the 8 files carrying
real messages, 2 are tasks and 6 are machine-driven analysis prompts the interpreter correctly
reads as conversations.

## Five produced WORKFLOWs, for a human to read

### `88bd8e61-6a1a-443f-a1db-615679fbfb85`

First ledger entry: *2026-09-13T23:24Z  why does eleven labs tell me i have no credits when i try to call eric but my account clearly shows 100,000 available credits? plea*

```
OUTCOME
Find out why ElevenLabs says there are no credits when calling Eric, and where the credits went. Then change the setup, for example by moving to a cheaper model, so this doesn't happen again, without spending more than the current subscription. Finally, set a wake-up for tomorrow at 7:40pm to run the tests.
KIND: task

# WORKFLOW
C1 UNDERSTAND — The current credit usage breakdown, the model in use for the Eric call, and the no-credits failure have all been observed
C2 BUILD — The configuration uses the chosen cheaper model or usage limits, and a wake-up is scheduled for tomorrow at 7:40pm to run the tests
C3 PROVE — every DONE WHEN item has a proof that reproduces
C4 VERIFY — a fresh-context verifier ran after the last change and returned PASS
```

### `01a0985c-69de-7370-835d-146baef52bea`

First ledger entry: *2026-09-13T01:23Z  create a file called ok.txt containing the word fine*

```
OUTCOME
Create a new file named ok.txt that contains the word "fine".
KIND: task

# WORKFLOW
C1 UNDERSTAND — whether ok.txt already exists in the working directory, and its current content if it does, has been observed
C2 BUILD — ok.txt exists in the working directory and its content is the word fine
C3 PROVE — every DONE WHEN item has a proof that reproduces
C4 VERIFY — a fresh-context verifier ran after the last change and returned PASS
```

### `692eabd1-a390-4029-83cf-d187b182d0b6`

First ledger entry: *2026-09-13T04:04Z  Summarize this portion of a Claude Code session transcript. Focus on:*

```
OUTCOME
Write a short summary of the given Claude Code session transcript chunk. It should cover what the user asked for, what Claude did, any friction, the outcome, and how the user phrased their requests.
KIND: conversation

# WORKFLOW
none (conversation)
```

### `d1f9b031-dd4c-4012-a3f6-000bbc499b6c`

First ledger entry: *2026-09-13T04:01Z  You are analyzing ONE Claude Code session transcript through the lens of an*

```
OUTCOME
Read the one Claude Code session transcript provided and return a single JSON object that describes how the operator directed the AI. The object has these fields: session theme, delegation mode, verification behaviours, autonomy pattern, incidents, operator moves, automation shipped and article hooks. Every field must rest on what the transcript actually shows.
KIND: conversation

# WORKFLOW
none (conversation)
```

### `5307a611-8d80-4988-9de4-21ae57f35d98_a074870617e15515d`

First ledger entry: *(empty ledger — a subagent copy)*

```
OUTCOME
No messages have been sent yet, so nothing has been asked for.
KIND: conversation

# WORKFLOW
none (conversation)
```

